@source: project.sh sync.sh remote.sh release.sh utils.sh

_tests:
    -
        -
            _info:
                title: Run a full test of sns
            _env:
                - RUN: TEST_FULL -- "@{DIR}"
